# Ground truth facts

- Live source now contradicts the previous failure summary for `verus-mimalloc/types.rs::todo`: both cfg variants carry `ensures false`, while the executable bodies still abort/panic (`verus-mimalloc/types.rs:3329-3345`). The earlier statement that both variants had no `ensures` clauses is stale.
- `research/operator_decisions.jsonl` entry `od-7` records that the operator rejected adding `loop { }` after the `todo()` calls and identified the intended fix as restoring `todo()`'s non-return contract. The live source now reflects that recorded restoration.
- `verus-mimalloc/os_mem.rs::mmap_prot_read_write` exposes all memory, alignment, and provenance facts only under the guard `pt.0.addr() != MAP_FAILED` (`verus-mimalloc/os_mem.rs:143-154`). With `todo()` carrying `ensures false`, Verus treats the `MAP_FAILED` branches in the init callers as non-returning.
- Current full verification no longer fails at the init mmap/todo frontier: `make verify` reports `verification results:: 884 verified, 0 errors`, covering the former `thread_data_alloc` postcondition at `verus-mimalloc/init.rs:1064` and `init_empty_page_ptr` precondition at `verus-mimalloc/init.rs:1227`.
- 2026-08-17 planning-cycle measurement: `make verify` exited 0; boundary_guard passed with 4 frozen goal targets; middle_guard spec-drift and ast-consistency passed; operator_decisions gate passed; `grep -c 'assume(false)' verus-mimalloc/page_organization.rs` returned 0.
- 2026-08-17 completion_gate measurement: completion is not done with 75 reachable proof-node obligations remaining; it specifically reports `verus-mimalloc/free.rs::free` as a goal target still TRUSTED/ADMITTED because of `#[verifier::external_body]`.
- 2026-08-17 proof_state measurement: `heap_init` and `heap_malloc` goal nodes are `unentered` with no direct trust leaves; `free` is `wip_trust` with a direct `external_body` trust leaf. The first verifier VC after removing `free`'s external_body marker has not been measured in this planning cycle.
- 2026-08-17 21:56 planning-cycle measurement: `make verify` exited 0 and reported `verification results:: 960 verified, 0 errors` with Verus `0.2026.06.28.1847ab3`.
- 2026-08-17 21:56 planning-cycle measurement: `$ARGUS_SKILL_PYTHON -m argus_skill.verticals.verus.tools.proof_state --crate-root . next` exited 0. `.verus_agent/proof_state.json` reports 192 reachable functions, 519 edges, 38 `wip_trust`, 1 `unentered`, 17 `unknown`, and 56 unresolved reachable obligations. The source top frontier includes project nodes `verus-mimalloc/alloc_fast.rs::heap_malloc_zero_ex@L77`, `verus-mimalloc/init.rs::init_empty_page_ptr@L1212`, and `verus-mimalloc/segment.rs::segment_page_clear@L1614`; the bottom frontier printed only builtin/vstd unknown nodes.
- 2026-08-17 21:56 planning-cycle measurement: `completion_gate --crate-root .` exited nonzero with `completion_gate: NOT DONE — derived proof obligations remain` and reported 56 reachable proof-node obligations; the first printed obligation was `unknown: Ghost::assume_new_fallback (.verus_deps/builtin/src/lib.rs:494)`.
- 2026-08-17 21:56 planning-cycle measurement: direct scans returned `grep -c 'assume(false)' verus-mimalloc/page_organization.rs` = 0 and `grep -c '{ true }' verus-mimalloc/page_organization.rs` = 0.
- 2026-08-17 21:56 planning-cycle measurement: `boundary_guard --crate-root . check` passed and `operator_decisions --project-root . gate` passed. This Planner attempt did not obtain a valid fresh `spec_drift` or `ast_consistency` run because the CLI/module layout expected by those wrappers was not rediscovered in this cycle; the latest reviewed handoff recorded both as passing after the bounded `segment_page_clear` repair.
- 2026-08-17 21:56 planning-cycle unknown: the first verifier VC after entering `verus-mimalloc/init.rs::init_empty_page_ptr` with `#[verus_verify]` has not been measured yet.

- 2026-08-19 planning-cycle #2 measurement: `make verify` exited 0 and reported `verification results:: 1384 verified, 0 errors` with Verus `0.2026.06.28.1847ab3`.
- 2026-08-19 planning-cycle #2 measurement: using `$ARGUS_SKILL_PYTHON`, `boundary_guard --crate-root . check`, `middle_guard --crate-root . spec-drift`, `middle_guard --crate-root . ast-consistency`, `operator_decisions --project-root . gate`, and `completion_gate --crate-root .` all passed; `proof_state --crate-root . next` printed `proof_state: no reachable unresolved frontier`.
- 2026-08-19 planning-cycle #2 direct scans: `grep -c 'assume(false)' verus-mimalloc/page_organization.rs` returned 0 and `grep -c '{ true }' verus-mimalloc/page_organization.rs` returned 0. A raw exact scan for `#[verifier::external_body]` returned 36 markers, and the reviewed checkpoint at `/users/jinlang/.argus-skill/projects/37bc66d4d753/handoffs/734b3f0b114d/CHECKPOINT.md` records a broader raw external-body gap of 22 versus the 19-marker target, reconciled to 17 unreachable unsanctioned proof-state nodes plus 5 raw text/cfg/macro/collapsed markers.
- 2026-08-19 planning-cycle #2 binding unknown: the reviewed checkpoint identifies `segment_free` as active on the `free_block -> page_retire -> page_free -> segment_page_free -> segment_free` path with an active `todo();` at `verus-mimalloc/segment.rs:3090-3094` and the apparent real implementation commented at `verus-mimalloc/segment.rs:3095-3137`; whether restoring that commented implementation is authorized requires an operator decision before proof work on that body.
